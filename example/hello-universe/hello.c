// loom:start(hello.c)
#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    // loom:start(hello.c/printf)
    printf("Hello, Universe!\n");
    // loom:end(hello.c/printf)
    return EXIT_SUCCESS;
}
// loom:end(hello.c)
